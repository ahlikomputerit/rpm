services:
  agent-lite:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - ./data:/app/data
    mem_limit: 2500m
    cpus: 2.0
    security_opt:
      - no-new-privileges:true
